{
  "name": "mean-6-task",
  "version": "1.0.0",
  "description": "Angular + Express проект",
  "scripts": {
    "build": "cd front/angular-app && npm install && npm run build -- --configuration production",    
    "start": "cd server && npm install && node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.13.2"
  }
}
